{
  "name": "job-matching",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@jobmatch/shared && npm run build --workspace=web && npm run build --workspace=api",
    "dev": "concurrently \"npm --workspace apps/web run dev\" \"npm --workspace apps/api run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
